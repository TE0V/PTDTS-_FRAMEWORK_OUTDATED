# PTDTS Requirements for Raspberry Pi 5
# Uses lgpio instead of pigpio for GPIO compatibility

# Core System
flask==2.3.3
flask-cors==4.0.0
flask-socketio==5.3.4
python-socketio==5.9.0

# GPIO Control (Pi 5 compatible)
gpiozero==2.0
lgpio==0.2.2  # Replaces pigpio for Pi 5 compatibility
# RPi.GPIO not recommended for Pi 5

# Computer Vision and AI
opencv-python==4.8.1.78
ultralytics==8.0.200
# Note: Torch may need manual installation for ARM64
# torch and torchvision can be installed from:
# https://torch.mlpack.org/ for better ARM optimization

# Scientific Computing
numpy==1.24.3
scipy==1.11.3
scikit-learn==1.3.1

# Kalman Filtering
filterpy==1.4.5

# Audio Processing
pyaudio==0.2.13
librosa==0.10.1
sounddevice==0.4.6

# Networking
pyzmq==25.1.1
requests==2.31.0
websocket-client==1.6.3

# Data Processing
pandas==2.1.1
matplotlib==3.8.0

# System Utilities
psutil==5.9.5
pyyaml==6.0.1
jsonschema==4.19.1

# Logging
colorlog==6.7.0
python-dotenv==1.0.0

# Camera support (should be pre-installed on Pi OS)
# picamera2==0.3.12

# Testing
pytest==7.4.2
pytest-cov==4.1.0
